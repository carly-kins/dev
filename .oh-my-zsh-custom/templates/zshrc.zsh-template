export ENV_PATH=~/dev
export DOTFILES_PATH="$ENV_PATH/.oh-my-zsh-custom/templates"

if [[ $(uname -o) == "GNU/Linux" ]]; then
  source "$DOTFILES_PATH/linux.sh"
else 
  source "$DOTFILES_PATH/macos.sh"
fi
